node_modules/
dist/
*.local
.DS_Store
.vite/

# Third-party 11+ familiarisation papers (© GL Assessment) — kept locally for
# reference only, never redistributed. See docs/latymer-alignment.md.
data/past papers/

# Generated by `npm run split`. free.json is bundled into the client; paid.json
# is fetched at runtime. Neither is committed — the build regenerates both.
src/data/free.json
public/paid.json

# The review sheet is the whole bank *with answers*. Generated by `npm run
# review` and kept local: publishing it would hand over the question bank.
docs/review-sheet.md
